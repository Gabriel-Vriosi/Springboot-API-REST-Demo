spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: org.h2.Driver

  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      dialect: H2Dialect
      ddl-auto: create-drop